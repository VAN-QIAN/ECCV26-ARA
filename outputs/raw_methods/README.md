# Raw Method Outputs

Raw full-run method predictions are not stored in Git. The scoring scripts
expect the filenames documented in `configs/method_paths.yaml`.

Run `scripts/setup_local_assets.sh` after creating `configs/paths.env` to
recreate local symlinks for the preserved raw outputs.
